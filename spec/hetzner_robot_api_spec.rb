require "spec_helper"

describe HetznerRobotApi do
  it "has a version number" do
    expect(HetznerRobotApi::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
